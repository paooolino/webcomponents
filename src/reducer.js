/*
    external imports
*/

/*
    internal imports
*/

import * as types from './actionTypes';

/*
    setup
*/

const initialState = {
    
};

/*
    reducer definition
*/

export default (state=initialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}
